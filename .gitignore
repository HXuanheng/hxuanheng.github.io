# Private working files — never publish to GitHub Pages.
# These stay on your local disk only; they are not tracked or served.

# Job-application drafts (cover letters, references, statements, checklists)
documents/

# Job applications — tracker + packets, private, Dropbox-only
jobmarket/

# References page — hidden until referees give consent. Re-enable by removing
# this line and restoring the nav links (see git history of nav edits).
references.html

# Teaching page — hidden for now. Re-enable by removing this line and restoring
# the nav links + sitemap entry.
teaching.html
